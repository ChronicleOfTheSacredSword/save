networks:
  pg-net:
    external: true  # Utiliser un réseau Docker existant

services:
  postgres:
    image: postgres:16
    container_name: heroes-postgres
    ports:
      - "5432:5432"
    networks:
      - pg-net
    environment:
      POSTGRES_USER: heroesdevuser
      POSTGRES_PASSWORD: heroesdev
      POSTGRES_DB: heroesdev
      TZ: Europe/Paris
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./postgres-init:/docker-entrypoint-initdb.d

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin4
    ports:
      - "5050:80"
    networks:
      - pg-net
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin

volumes:
  pgdata:
